{
  "name": "stt-summary-server",
  "private": true,
  "workspaces": [
    "packages/*",
    "shared"
  ],
  "scripts": {
    "test": "npm run test --workspace=packages/server && npm run test --workspace=packages/worker"
  }
}
